services:
  server:
    build: server
    restart: unless-stopped
    ports:
      - 8000:8000
    volumes:
      - ./server:/app
    env_file:
      - project.env
  


  client:
    build: client
    restart: unless-stopped
    env_file:
      - project.env
    volumes:
      - ./client:/app
    ports:
      - 5173:5173
    depends_on:
      - server
